# (!) Requires project01 files and node_modules in root folder (available separately)

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

(!) Only App.js is refactored
